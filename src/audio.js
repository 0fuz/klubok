// Sound placeholders. Hook real Web Audio synthesis in here later; the game never
// depends on these returning anything.
export const Sfx = {
  enabled: false,
  tap() {},
  exit() {},
  bump() {},
  win() {},
  lose() {},
};
