{
  "name": "klubok",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Snake untangle puzzle: tap snakes to let them slither out of the board",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "bench": "node tools/bench.js",
    "show": "node tools/show.js",
    "serve": "python3 -m http.server 8080"
  }
}
